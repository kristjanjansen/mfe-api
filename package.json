{
  "type": "module",
  "scripts": {
    "build": "nitro build",
    "dev": "nitro dev",
    "preview": "npx srvx --prod .output/ --port 4000 -host 0.0.0.0"
  },
  "devDependencies": {
    "nitro": "latest",
    "rolldown": "latest"
  }
}
